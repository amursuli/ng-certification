import { ImagesWeatherPipe } from './images-weather.pipe';

describe('ImagesWeatherPipe', () => {
  it('create an instance', () => {
    const pipe = new ImagesWeatherPipe();
    expect(pipe).toBeTruthy();
  });
});
